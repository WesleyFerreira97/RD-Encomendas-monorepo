import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    inputContainer: {
        height: 60,
        width: '80%',
        marginTop: -30,
        alignSelf: 'center',
    },
    inputStyle: {
        width: '100%',
        // background: '#000',
    }
});